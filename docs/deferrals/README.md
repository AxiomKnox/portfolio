# Deferrals

**Active deferrals only.** Park work that is intentionally not-now here so it has one home.

## Intent

- This folder is for **still-open** deferred items (briefs, pointers, short notes).
- **Completed, canceled, or superseded** work does **not** belong here — leave those in `docs/research/`, `docs/additional-info/`, Linear, or delete/archive elsewhere.
- Process (naming, when to promote to a Linear issue, when to prune) is **TBD**. Prefer a short file over a perfect taxonomy.

## Existing deferred material (not moved yet)

Older deferral briefs still live under `docs/additional-info/` and `docs/research/` (e.g. Starwind/theme-toggle notes in the grouped data/Starwind brief). Relocate into this folder only when it is clearly still active and links won’t break badly.

**PROG-87** (Starwind-related deferred work, if/when tracked) can live here as a short pointer file when useful — do not overbuild a mirror of Linear.

## Suggested shape (optional)

```text
docs/deferrals/
  README.md          ← this file
  <short-slug>.md    ← one active deferral per file when you add one
```
