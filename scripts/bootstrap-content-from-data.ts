/**
 * Historical one-shot used to seed `src/content/**` from the old `src/data/*.ts` fixtures.
 * Fixtures now live under `src/content/projects/*` and `src/content/profile/`.
 * Re-run is a no-op unless you restore TS fixtures and re-enable the generator body.
 */
console.log(
  "Bootstrap already applied — edit src/content/** directly (or restore from git history).",
);
